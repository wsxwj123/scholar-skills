# USAGE_GUIDE（已作废，待删除）

本文件原先是一份 v2.0 时代的端到端演示（`/init` → `/preview` → `/storyline` →
`/write` → `/check` → `/merge` 的对话样例）。**那套流程已经不存在了**：现行流程是
Phase 0–16，`/check` 是 11 步的多层核查、storyline 有结构签字硬门禁、
`/journal-study` 已停用。照这份文件操作只会走进不存在的路。

演示过时的文档比没有文档更有害，所以内容已清空，不做"修到看起来对"——
它里面唯一还准确的部分（citation_guard 命令、`set-field`）在下面两处都有，
重写一份只会再造一个漂移源。

**真正的入口：**

| 你要什么 | 看哪里 |
|---|---|
| 完整流程与每步的阻断条件 | `SKILL.md`（Phase 0–16） |
| 常用命令速查 | `QUICK_REFERENCE.md` |
| 这技能是干什么的 / 配置怎么设 | `README.md` |
| 哪些文件是脚本生成的 | `RUNTIME_LAYOUT.md` |
| 改脚本要保证哪些行为不倒退 | `TEST_CHECKLIST.md` |
